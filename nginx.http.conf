# Plain-HTTP config, used only when DISABLE_INTERNAL_TLS=true (i.e. a
# TLS-terminating reverse proxy fronts this container).
server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
